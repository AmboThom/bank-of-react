import React, { useState } from "react";
import "../App.css";

// TODO: Remove this comment after -- feel free to use FormControl via material-ui since lib is already imported
// or use react-hook-form library or regular html forms 

const Form = (props) => {
    return (<h1>Did this get through?</h1>); // Placeholder
};

export default Form;